<!-- Defining namespaces and importing-->

<UserControl x:Class="Risk.View.UserControl.EditorWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:model="clr-namespace:Risk.Model"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Risk.View.UserControl"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"

             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <!-- Defining datatypes and resources-->

    <UserControl.Resources>
        <ResourceDictionary>
            <DataTemplate DataType="{x:Type model:dataContainer}">
                <Rectangle Width="50" Height="50" Fill="Green"></Rectangle>
            </DataTemplate>
        </ResourceDictionary>
    </UserControl.Resources>

    <!-- Docking-->
    <DockPanel>
        <Canvas DockPanel.Dock="Bottom">
            <!-- Setting a background picture. Should be done by using a binding to an element.-->
            <!--
            <Canvas.Background>
                <ImageBrush ImageSource="/Images/RiskMapDummy.png"/>
            </Canvas.Background>
            -->
            
<!--            <ContentPresenter Content="{Binding Content}" OpacityMask="Black" Canvas.Left="1" Canvas.Top="1" Height="160" RenderTransformOrigin="0.5,0.5" Width="300"   >
                <ContentPresenter.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform ScaleY="-1" ScaleX="-1"/>
                        <SkewTransform/>
                        <RotateTransform/>
                        <TranslateTransform/>
                    </TransformGroup>
                </ContentPresenter.RenderTransform>-->
                <i:Interaction.Behaviors>
                    <!-- <components:DragBehavior></components:DragBehavior>-->
                </i:Interaction.Behaviors>
           <!-- </ContentPresenter>-->
            <local:CountryButtonEditor Canvas.Left="100" Canvas.Right="100">
                
            </local:CountryButtonEditor>
        </Canvas>
    </DockPanel>


</UserControl>
